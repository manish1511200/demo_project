<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Drawings page</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
 
    <style>
        table, th, tr, td {
            border: 1px solid red;
            border-collapse: collapse;
        }
    </style>
</head>
<body>
        <h2 class="text-center" style="color:blue"> Drawing Page</h2>
    <div class="container-fluid">
        <div class="row justify-content-end">
            <div class="col-md-12">
                <div id="drawings-section" class="container mt-4">
                    <h2  style="color:orange">Drawings Listing With AJax</h2>
                      
                        <br>
                    <table class="table">
                        <thead>
                         {% if user_type != "View" %} 
                            <tr>
                                    <th>Id:</th>
                                    <th>Project_Name</th>
                                    <th>Drawing No</th>
                                    <th>Drawing Name</th>
                                    <th>Status</th>
                                    <th>Category</th>
                                    <th>File</th>
                                 {% endif %}
                                <th colspan="2">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for viewDrawingByProjects  in value %}
                                <tr id="delete-row-{{ viewDrawingByProjects.id }}">
                                     <td>{{ viewDrawingByProjects.id }}</td>
                                     <td>{{ viewDrawingByProjects.project_name }}</td>
                                    <td>{{ viewDrawingByProjects.drawing_number }}</td>
                                    <td>{{ viewDrawingByProjects.drawing_name }}</td>
                                    <td>{{ viewDrawingByProjects.drawing_status }}</td>
                                    <td>{{ viewDrawingByProjects.category }}</td>
                                    <td>
                                        <a href="../upload/document/{{ viewDrawingByProjects.pdf_file }}">
                                            <i class="fa fa-file-pdf-o" style="font-size:20px;color:red"></i>
                                        </a>
                                    </td>
                                   {% if user_type == "Standard" or user_type == "Admin" %}
                                        <td align="center">
                                            <a class="btn btn-success" href="editDrawings.php?id={{ viewDrawingByProjects.id}}">Edit</a>
                                        </td>
                                    {% endif %}

                                    {% if user_type == "Admin" %}
                                        <td align="center">
                                            <a id="delete-btn-{{ viewDrawingByProjects.id }}" class="btn btn-success" onclick="check_delete({{ viewDrawingByProjects.id }})" style="background-color: red;" href="javascript:;">Delete</a>
                                        </td>
                                    {% endif %}
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    </div>
</body>
</html>